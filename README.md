# FreezeBox — curated profiles

Public catalog of curated "game profiles" for the FreezeBox app. Edit
`profiles.json` and commit — the app fetches the latest on next launch, no app
release needed.

Each entry: `{ id, name, icon, packages (freeze), keepActive, launch? }` using
real Android package names.
